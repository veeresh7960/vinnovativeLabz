//name : veeresh  phone : 8217096590

function findPair(arr,key){
    console.log("invoke  consecutivePairs")
    console.log(arr)
    console.log(key)
    var a
    var b[]
    for(var i=0;i<Array.length;i++){
        a=i+1;
        if(arr[i]+arr[a]==key){

            b.push[i]
            b.push[a]
            return b

        }
    }
}
var arr=[4,6,5,7,3,3,9,1,4]
var key=5
console.log(findPair(arr,key))
